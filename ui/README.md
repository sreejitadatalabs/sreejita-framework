# Sreejita Framework — Streamlit UI (v1.9)

This is a **demo UI** built on top of the Sreejita Framework v1.8+.

## Purpose

- Provide a visual demo for freelancing & LinkedIn
- Expose existing backend capability
- No new analytics logic

## How to Run Locally

From the project root:

```bash
pip install -r requirements.txt
streamlit run ui/app.py
